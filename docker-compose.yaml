networks:
  beacon-network:

x-environment: &env
  - POSTGRES_USER=postgres
  - POSTGRES_PASSWORD=postgres
  - POSTGRES_BN_DATABASE=beacon-network
  - POSTGRES_KC_DATABASE=keycloak
  - KC_DB_URL=jdbc:postgresql://postgres:5432/keycloak
  - KC_DB_USERNAME=keycloak
  - KC_DB_PASSWORD=keycloak
  - KC_HTTP_RELATIVE_PATH=auth
  - KC_HOSTNAME_STRICT=false
  - KC_PROXY=edge
  - KEYCLOAK_ADMIN=admin
  - KEYCLOAK_ADMIN_PASSWORD=yaumamydurochka
  - BEACON_NETWORK_CONFIG_DIR=/wildfly/BEACON-INF
  - BEACON_NETWORK_DB_CONNECTION_URL=jdbc:postgresql://postgres:5432/beacon-network
  - BEACON_NETWORK_DB_USERNAME=beacon
  - BEACON_NETWORK_DB_PASSWORD=beacon
  - JBOSS_JAVA_SIZING=-XX:+UseContainerSupport -XX:MaxRAMPercentage=70.0 -Xms128m
services:
  postgres:
    image: postgres:16.0-alpine
    container_name: "bn-db"
    restart: always
    environment: *env
    ports:
      - '5432:5432'
    volumes:
      - ./postgresql/data:/var/lib/postgresql/data
      - ./postgresql/init.sh:/docker-entrypoint-initdb.d/init.sh:ro
    networks:
      - beacon-network

  wildfly:
    image: ghcr.io/elixir-europe/beacon-network-backend-oidc:v0.0.13-oidc
    container_name: "bn-server"
    depends_on:
      - postgres
    restart: always
    volumes:
      - ./wildfly/BEACON-INF:/wildfly/BEACON-INF
    hostname: wildfly
    networks:
      - beacon-network
    expose:
      - '8080'
    environment: *env

  frontend:
#    image: ghcr.io/elixir-europe/beacon-network-ui:latest
    image: ghcr.io/elixir-europe/beacon-network-ui@sha256:9cf7151344d16d21edf1de1972192b51da24b0ad1cb0b3863d1a8e77802fc63d
    command: node /opt/yarn-v1.22.19/bin/yarn.js start
    hostname: frontend
    container_name: "bn-ui"
    environment:
      - CHOKIDAR_USEPOLLING=true
      - REACT_APP_CLIENT_ID=LS-LOGIN-CLIENT-ID
      - REACT_APP_CLIENT_SECRET=LS-LOGIN-CLIENT-SECRET
      - REACT_APP_KEYCLOAK_CLIENT_ID=beacon-network-ui
      - REACT_APP_KEYCLOAK_CLIENT_SECRET=iXebYRoJitC1w6tv5SJppFQgNBmKyupZ
      - REACT_APP_KEYCLOAK_CLIENT_REALM=beacon-network
    volumes:
      - ./frontend/.env:/frontend/.env
      - ./frontend/config.json:/frontend/src/config.json:ro
    networks:
      - beacon-network
    expose:
      - '3000'
    stdin_open: true

  keycloak:
    container_name: keycloak
    environment: *env
    volumes:
      - ./keycloak/import:/opt/keycloak/data/import
    ports:
      - '8080'
    image: quay.io/keycloak/keycloak:25.0.1
    networks:
      - beacon-network
    depends_on:
      - postgres
    command:
      - start-dev
      - --db
      - postgres
      - --import-realm
      - --features
      - preview
  nginx:
    image: nginx
    container_name: "bn-proxy"
    depends_on:
      - wildfly
    restart: always
    volumes:
      - ./nginx/default.conf:/etc/nginx/conf.d/default.conf:ro
    networks:
      - beacon-network
    ports:
      - '8080:8080'